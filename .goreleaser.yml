version: 2

builds:
  - binary: uqpay
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/uqpay/uqpay-cli/internal/build.Version={{ .Version }} -X github.com/uqpay/uqpay-cli/internal/build.Date={{ .Date }}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - name_template: "uqpay-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
